## python basic
* print() 형식으로 글자 출력 가능
* 변수 선언은 a = 10 같은 식으로 가능
* f'{}{}' 식으로 작성할 시 변수를 안에 간편하게 넣을 수 있음
* 함수는 객체...?의 뒤에 .함수명 같은 식으로 사용 가능
* swap() 함수는 두 개의 변수를 교환
* unpacking은 두 개의 변수를 해체하는 것 (ex: n1,n2 = [100,200] print(n1) print(n2))

## dictionary
* dict는 my_dict{'key':'val'} 과 같은 형식으로 선언 가능
* get 함수: dict에서 키에 대응하는 값을 가져옴

## 반복문
* for v in my_dict.items(): 와 같은 형식으로 선언한다
* while의 경우 while i <= 5: 와 같은 형식으로 선언한다

## list
* my_list = list()와 같은 형식으로 선언
* append()는 리스트의 맨 끝에 내용을 추가
* extend()는 여러 내용을 추가
